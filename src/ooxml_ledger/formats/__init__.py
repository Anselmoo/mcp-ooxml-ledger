"""Per-format addressing, application and reversal. One module per format.

`wml.py` is Word, `pml.py` PowerPoint, `sml.py` Excel — the split named in design §4. `sml.py`
does not exist yet; SpreadsheetML shares none of this run-fragmentation machinery and is a
separate plan.
"""
